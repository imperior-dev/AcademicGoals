type statusType = "loading" | "loaded";

type pageType = "home" | "setting" | "homework" | "analytics" | "schedule";

type screenType = "home" | "setup" | "input" | "other";

export { statusType, pageType, screenType };
